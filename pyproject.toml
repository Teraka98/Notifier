[project]
name = "notifier-fastapi-app"
version = "0.1.0"
description = "FastAPI application"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "loguru"
]

[tool.uv]
dev-dependencies = [
    "pytest",
    "httpx",
]

